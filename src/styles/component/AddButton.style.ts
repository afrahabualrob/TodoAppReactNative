import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  buttonContent: {
    flexDirection: 'row',
    alignItems: 'center',
    marginHorizontal: 20,
  },
  buttonText: {
    marginLeft: 8,
    fontSize: 18,
    color: '#aaa',
  },
});

export default styles;
