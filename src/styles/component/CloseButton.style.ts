import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  closeButtonText: {
    fontSize: 18,
    color: '#FFF',
  },

  closeButton: {
    alignItems: 'flex-end',
  },
});

export default styles;
